<?xml version='1.0'?>
<!DOCTYPE book PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [<!ENTITY % BOOK_ENTITIES SYSTEM "JAIN_SLEE_Example_User_Guide.ent">%BOOK_ENTITIES;]>

<chapter id="running">

	<title>Running the Example</title>
	
	<para>To easiest way to try the example application is to start the JAIN SLEE container, then use SIPP scripts, <filename>run*.sh</filename> or <filename>run*.bat</filename> depending on which Operating System being used, in <filename>sipp</filename> directory.</para>
	
	<para>The scripts provide two different session flows provided, successful call handling (the <filename>run_*_DIALOG.*</filename> scripts) and call cancellation (the <filename>run_*_CANCEL.*</filename> scripts). For each scenario first start the <literal>uas</literal> script, then the <literal>uac</literal> script, which triggers the calls. All the traffic should be printed in the application server console. The usage of SIPP scripts requires SIPP to be in <literal>$PATH</literal> environment variable.</para> 
		
</chapter>

