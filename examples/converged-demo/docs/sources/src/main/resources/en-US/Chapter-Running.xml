<?xml version='1.0'?>
<!DOCTYPE book PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [<!ENTITY % BOOK_ENTITIES SYSTEM "JAIN_SLEE_Example_User_Guide.ent">%BOOK_ENTITIES;]>

<chapter id="running">

	<title>Running the Example</title>

	<para>This chapter container brief instructions how to run example.
	</para>
	<section id="running_base_conf">
		<title>Basic configuration</title>
		<para>
			Before running example ensure that all
			<acronym>SIP</acronym>
			addresses configured via
			<acronym>SBB</acronym>
			environment entries are valid.
		</para>
		<para>
			In binary release please edit
			<filename>sbb-jar.xml</filename>
			to ensure proper values. Since &THIS.EXAMPLE;
			Example does not depend on
			<literal>Registrar</literal>
			<acronym>URI</acronym>
			s must be direct address of
			<acronym>UA</acronym>
			.
		</para>
	</section>
	<section id="running_user">
		<title>&THIS.EXAMPLE;
			Regular customer
		</title>
		<para>Please follow procedure below:</para>
		<procedure>
			<step>
				<title>Install</title>
				<para>Install dependencies and example.</para>
			</step>
			<step>
				<title>UA Configuration</title>
				<para>Start <acronym>SIP</acronym> <acronym>UA</acronym>s. Ensure admins <acronym>UA</acronym> is bound to previously configured <acronym>SIP</acronym> <acronym>URL</acronym>.</para>
			</step>
			<step>
				<title>Start</title>
				<para>
					Start container, for instance with such command:
					<command>$ bin/run.sh -b 127.0.0.1 -c default</command>
				</para>

			</step>
			<step>
				<title>Access web interface</title>
				<para>
					Access following address with browser:
					<command>http://127.0.0.1:8080/ShoppingDemo</command>
				</para>
				<para>You should see: </para>
				<!--Docs:  This exceeds page ? -->
				<mediaobject>
					<imageobject>
						<imagedata width="300px" align="center"
							fileref="images/run-user1.png" format="PNG" />
					</imageobject>
					<caption>
						<para>&THIS.EXAMPLE;
							main screen
						</para>
					</caption>
				</mediaobject>
			</step>
			<step>
				<title>Create account</title>
				<para>Create account - fill all required data, ensure that <literal>Phone</literal> points to <acronym>SIP</acronym> <acronym>UA</acronym>(for instance <literal>sip:shop_lifter@127.0.0.1:5060</literal> ).</para>
			</step>
			<step>
				<title>Start shopping</title>
				<para>Make orders and checkout, wait for calls.</para>
			</step>
		</procedure>
	</section>
	<section id="running_admin">
		<title>&THIS.EXAMPLE;
			Administrator
		</title>
		<para>AMIT</para>
	</section>
</chapter>

