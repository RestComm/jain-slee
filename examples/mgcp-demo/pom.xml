<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    
    <parent>
        <artifactId>mobicents-jainslee-server</artifactId>
        <groupId>org.mobicents</groupId>
        <version>1.2.0.GA-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>
    
    <artifactId>mgcp-demo-parent</artifactId>
    <groupId>org.mobicents.examples</groupId>
    
    <packaging>pom</packaging>
    <name>MGCP :: ${pom.artifactId}</name>
    
    <url>http://maven.apache.org</url>
    
    <modules>
        <module>call-sbb</module>
	<module>crcx-sbb</module>
	<module>crcx-endpoint-sbb</module>
	<module>mdcx-sbb</module>
        <module>services-du</module>		
    </modules>
    
    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <inherited>false</inherited>
                        <executions>
                            <execution>
                                <phase>install</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <tasks>
                                        <copy overwrite="true" todir="${basedir}/release/${release.name}/mgcp-demo">
                                            <fileset dir="${basedir}/events-du/target">
                                                <include name="**.jar" />
                                                <include name="*.xml" />
                                                <include name="*.properties" />
                                            </fileset>
                                            <fileset dir="${basedir}/services-du/target">
                                                <include name="**.jar" />
                                                <include name="*.xml" />
                                                <include name="*.properties" />
                                            </fileset>
                                        </copy>
                                        <copy overwrite="true" file="${basedir}/events-du/target/build.xml" tofile="${basedir}/release/${release.name}/mgcp-demo/events-du-management.xml" />
                                        <copy overwrite="true" file="${basedir}/services-du/target/build.xml" tofile="${basedir}/release/${release.name}/mgcp-demo/services-du-management.xml" />
                                        <copy overwrite="true" file="${basedir}/release/build.xml" todir="${basedir}/release/${release.name}/mgcp-demo" />
                                    </tasks>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
    
</project>