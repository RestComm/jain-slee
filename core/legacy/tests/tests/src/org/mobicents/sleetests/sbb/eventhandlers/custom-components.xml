<?xml version="1.0"?>

<project name="PT Inovação JAIN SLEE TCK -- test for initial event selector" default="package">
  <property name="test.dir" value="org/mobicents/sleetests/sbb/eventhandlers"/>
  <property name="test.src" value="${tck.source}/${test.dir}"/>
  <target name="package">
    <echo>classes.sleetests=${classes.sleetests}</echo>
    <deployablejar destfile="${jars.deployable-units}/TestCustomAciHandlerService-du.jar" servicexml="${test.src}/TestCustomAciHandler-META-INF/service-jar.xml">
      <sbbjar sbbjarxml="${test.src}/TestCustomAciHandler-META-INF/sbb-jar.xml"
                classpath="${classes.sleetests}">    	
       <fileset dir="${classes.sleetests}">
       		<include name="${test.dir}/**/*"/>
       	</fileset>
      </sbbjar>
    </deployablejar>
  </target>
</project>
