This is simple Resource Adaptor that takes Message as input and fires it to the SBB. 
To Deploy call 'mvn install'
To Un Deploy call 'mvn clean'

To start the swing client to fire events call 'ant'

To understand the RA look at RA api
