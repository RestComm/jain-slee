<?xml version="1.0" encoding="UTF-8"?>

<!-- $Id: jboss-service.xml,v 1.1 2007/09/04 19:02:42 abhayani Exp $ -->

<!-- ===================================================================== -->
<!--  JBoss Server Configuration                                           -->
<!-- ===================================================================== -->

<server>   
<mbean code="org.mobicents.slee.management.rules.RulesScanner"
      name="rulesra:service=RulesScanner">
 	
      <!-- Filter which only allows .xls, .XLS, .csv, .CSV and .drl files to be scanned for update-->
      <attribute name="Filter">org.mobicents.slee.management.rules.RulesFileFilter</attribute>
      <attribute name="ScanPeriod">5000</attribute>
      <attribute name="ScanEnabled">true</attribute>

      <!-- URLs are comma separated and resolve relative to the server home URL
         unless the given path is absolute. If the URL ends in "/" it is
         considered a collection and scanned, otherwise it is simply deployed;
         this follows RFC2518 convention and allows discrimination between
         collections and directories that are simply unpacked archives.

         URLs may be local (file:) or remote (http:). Scanning is supported
         for remote URLs but unpacked deployment units are not.

         Example URLs:
            deploy/
                 scans ${jboss.server.url}/deploy/, which is local or remote
                 depending on the URL used to boot the server
            ${jboss.server.home.dir}/deploy/
                 scans ${jboss.server.home.dir}/deploy, which is always local
            file:/var/opt/myapp.ear
                 deploy myapp.ear from a local location
            file:/var/opt/apps/
                 scans the specified directory
            http://www.test.com/netboot/myapp.ear
                 deploys myapp.ear from a remote location
            http://www.test.com/netboot/apps/
                 scans the specified WebDAV location
                 file://D:/mobicents/mobicents-examples/rules-sbb/spreadsheet/
       -->
      <attribute name="URLs">
         data/
      </attribute>

      <attribute name="RecursiveSearch">True</attribute>

    </mbean>
    
</server>
